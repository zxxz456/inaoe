# Entorno de las actividades de Fundamentos para el Analisis Automatico
# de Lenguaje. Se recrea con:
#
#   /opt/homebrew/bin/python3.13 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt
#
# El .venv no se versiona, este archivo si. Las versiones van fijadas
# para que la entrega corra igual en otra maquina.

ipykernel==7.3.0
nltk==3.10.3
pandas==3.0.5
